#[warn(unused_imports)]
#[warn(unused_variables)]
#[warn(unused_mut)]
use ndarray::arr2;
use crate::setup::{Game, entity::Entity};
use crow::Context;
pub fn create_from_template(mut ctx: &mut Context, mut game: &mut Game) {
    //no best entities for current 5 x 10 hidden layers - therefore empty
}